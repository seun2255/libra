{
  "name": "hardhat-project",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile && node scripts/updateContract.js",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy-testnet": "npx hardhat run scripts/deploy.js --network matic",
    "deploy-filecoin": "npx hardhat run scripts/deploy.js --network filecoin",
    "node": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@tableland/local": "^2.0.2",
    "hardhat": "^2.17.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "@tableland/evm": "^4.3.0",
    "dotenv": "^16.3.1",
    "hardhat-deploy": "^0.11.37",
    "hardhat-deploy-ethers": "^0.4.1"
  }
}
